import java.util.*;
public class MedianofTwoSortedArrays {
	public double findMedianSortedArrays(int[] A, int[] B) {
		int target = (A.length + B.length) / 2;
		int start1 = A.length / 2;
		while (true)
	}
}